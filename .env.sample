# AWS Bedrock Configuration for Claude Code
CLAUDE_CODE_USE_BEDROCK=1
# AWS_BEARER_TOKEN_BEDROCK=api-key-here  # Optional if using ~/.aws/credentials
AWS_REGION=your-aws-region
ANTHROPIC_MODEL=us.anthropic.claude-sonnet-4-20250514-v1:0

# Other AI APIs (optional)
OPENAI_API_KEY=
GEMINI_API_KEY=

# Claude Code CLI path (defaults to 'claude')
# If already installed locally, try:
# export PATH="$HOME/node_modules/.bin:$PATH"
# If installed locally, set to: /path/to/node_modules/.bin/claude
CLAUDE_CODE_PATH=claude

# Google Cloud Configuration for image generation workflows
# Use relative path when running with ./start.sh (recommended)
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
# Or use absolute path: GOOGLE_APPLICATION_CREDENTIALS=/full/path/to/service-account-key.json
GOOGLE_CLOUD_PROJECT=gcp-rnd-chatbot-1783-poc-ee44
GOOGLE_CLOUD_REGION=us-central1
GOOGLE_IMAGE_MODEL=imagen-4.0-ultra-generate-preview-06-06

# Alternative: Replicate API for image generation workflows (if not using Google)
# REPLICATE_API_TOKEN=

# ===========================
# ENHANCED LOGGING & MONITORING
# ===========================

# Notification Webhook Configuration
# Set webhook URL for Slack, Discord, or other webhook services
NOTIFICATION_WEBHOOK_URL=

# Minimum notification level: info, warning, error, critical
NOTIFICATION_MIN_LEVEL=error

# Webhook timeout in seconds
NOTIFICATION_TIMEOUT=10

# Health Check Server Configuration
# Port for health check endpoints (default: 8080)
HEALTH_SERVER_PORT=8080

# Health server host (default: 127.0.0.1)
# Use 0.0.0.0 to allow external connections
HEALTH_SERVER_HOST=127.0.0.1
